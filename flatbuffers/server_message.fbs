include "permission.fbs";
include "resultset.fbs";

namespace BlazingDB.Protocol;

table ServerMessage {
  statement:string;
  success:bool;
  message:string;
  resultset:ResultSet;
  authorization:string; // grant and revoke statement
}

root_type ServerMessage;
